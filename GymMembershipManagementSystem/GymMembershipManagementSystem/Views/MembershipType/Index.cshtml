@model IEnumerable<GymMembershipManagementSystem.Models.MembershipType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<style>
    body {
        /* Linear gradient from top (red) to bottom (blue) */
        background: linear-gradient(to bottom, #bcdaea, #15f5e6);
        /* Additional CSS properties for styling */
        width: 100%;
        height: 100%;
        /* Other styles as needed */
    }

    .form-group {
        padding-left: 550px;
    }
</style>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MembershipName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MembershipDuration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MembershipDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fee)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MembershipName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MembershipDuration)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MembershipDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fee)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MembershipId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MembershipId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MembershipId })
        </td>
    </tr>
}

</table>
